apiVersion: kibana.k8s.elastic.co/v1
kind: Kibana
metadata:
  name: quickstart
spec:
  version: 8.17.5
  count: 1
  elasticsearchRef:
    name: quickstart
  config:
    server.publicBaseUrl: https://search.dev.wgbh-mla.org
  http:
    tls:
      selfSignedCertificate:
        disabled: true
